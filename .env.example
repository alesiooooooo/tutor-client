# API Configuration
# For server-side (Server Actions)
API_BASE_URL=http://localhost:3010

# Docker Setup:
# - Linux VPS: use docker-compose.yml (uses 172.17.0.1:3010)
# - Mac/Windows: use docker-compose.mac.yml (uses host.docker.internal:3010)
#   Command: docker compose -f docker-compose.mac.yml up

# For client-side (must start with NEXT_PUBLIC_)
NEXT_PUBLIC_API_BASE_URL=http://localhost:3010

# Port for Next.js dev server
PORT=3001
